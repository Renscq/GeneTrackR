% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_ld_block.R
\name{plot_ld_block}
\alias{plot_ld_block}
\title{Plot a triangular LD heatmap}
\usage{
plot_ld_block(
  object,
  chrom = NULL,
  start = NULL,
  end = NULL,
  variant_type = c("both", "snp", "ind"),
  method = c("r2", "Dprime"),
  color_palette = "Reds",
  font = 14,
  title = NULL,
  label_by = c("pos", "variant_id"),
  show_variant_labels = TRUE,
  show_region = FALSE,
  annotation = NULL,
  connect_region = TRUE,
  region_height = 1.25,
  connector_height = 0.35,
  heatmap_height = NULL,
  samples = NULL,
  min_pair_samples = 3L,
  ploidy = 2L,
  verbose = TRUE,
  return_object = FALSE
)
}
\arguments{
\item{object}{An \code{LDTrack} object, a \code{VariantTrack} object, or a VCF path.}

\item{chrom, start, end}{Optional region used when \code{object} is not an \code{LDTrack}.}

\item{variant_type}{One of \code{both}, \code{snp}, or \code{ind} when computing from VCF.}

\item{method}{LD method used when computing from VCF.}

\item{color_palette}{RColorBrewer sequential palette name. Default \code{Reds}.}

\item{font}{Base font size. Font color is always black.}

\item{title}{Plot title. Default is \verb{chrom:start-end LD}.}

\item{label_by}{Variant label column, either \code{pos}, \code{variant_id}, or another
column in \code{LDTrack$variants}.}

\item{show_variant_labels}{Logical. Whether to show labels above the heatmap.}

\item{show_region}{Logical. Whether to add a compact region/variant track
above the LD heatmap.}

\item{annotation}{Optional gene annotation used by the compact region track.}

\item{connect_region}{Logical. Whether to connect region-track variant
positions to heatmap columns.}

\item{region_height}{Relative height of the compact region track.}

\item{connector_height}{Relative height of connector lines.}

\item{heatmap_height}{Relative height of the heatmap. If NULL, it is chosen
from the number of variants.}

\item{samples, min_pair_samples, ploidy, verbose}{Passed to \code{compute_ld_block()}
when \code{object} is not already an \code{LDTrack}.}

\item{return_object}{Logical. If TRUE, return an updated \code{LDTrack} containing
the plot in \verb{$plot}; otherwise return the plot.}
}
\value{
A ggplot/patchwork plot, or an updated \code{LDTrack} when \code{return_object = TRUE}.
}
\description{
Draws an inverted triangular LD heatmap using ggplot2. Interior grid lines are
suppressed; only the outside triangular frame is drawn. The function accepts
either an \code{LDTrack} object or a \code{VariantTrack}/VCF path, in which case LD is
computed first by \code{compute_ld_block()}. When \code{show_region = TRUE}, the region
track follows the compact gene-model style used by \code{plot_hap_variant()} in
GeneTrackR 0.3.15 and connects genomic variant markers to LD heatmap columns
with a shared x scale, so line endpoints remain aligned after resizing.
}
