% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read_genepred.R
\name{read_genepred}
\alias{read_genepred}
\title{Read a GenePred annotation file}
\usage{
read_genepred(
  file,
  format = c("auto", "genePred", "genePredExt"),
  coordinate = c("ucsc", "granges"),
  remove_invalid = TRUE,
  report_invalid = TRUE,
  gene_col = c("name2", "name"),
  transcript_col = "name",
  verbose = TRUE,
  progress = interactive() && isTRUE(verbose)
)
}
\arguments{
\item{file}{GenePred or GenePredExt file path.}

\item{format}{Input format. Use auto, genePred, or genePredExt.}

\item{coordinate}{Input coordinate system. ucsc means 0-based half-open.}

\item{remove_invalid}{Remove invalid records after validation.}

\item{report_invalid}{Store invalid records in the object validation slot.}

\item{gene_col}{Gene identifier source. name2 is recommended for GenePredExt.}

\item{transcript_col}{Transcript identifier source.}

\item{verbose}{Logical. Whether to print step-level progress messages. Default TRUE.}

\item{progress}{Logical. Whether to show a stage-level progress bar. By default, a progress bar is shown only in interactive sessions when \code{verbose = TRUE}.}
}
\value{
A GenePred object.
}
\description{
Read a GenePred annotation file
}
\details{
Standard GenePred uses \code{name} as transcript ID. GenePredExt commonly uses
\code{name} as transcript ID and \code{name2} as gene ID, so the default \code{gene_col}
tries \code{name2} first and falls back to \code{name} when \code{name2} is unavailable.
\code{coordinate = "ucsc"} converts 0-based half-open GenePred coordinates to
the package's internal 1-based closed coordinate system.

For large files, \code{verbose = TRUE} prints major processing stages, while
\code{progress = TRUE} additionally shows a stage-level text progress bar.
}
\examples{
gp_file <- system.file("extdata", "gtr_demo.genePredExt", package = "GeneTrackR")
gp <- read_genepred(gp_file, format = "genePredExt", verbose = FALSE)
gp
head(gp$genes)
head(gp$transcripts)
}
