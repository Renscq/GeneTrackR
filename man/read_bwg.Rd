% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read_bwg.R
\name{read_bwg}
\alias{read_bwg}
\title{Read bedGraph, wig, or bigWig signal tracks}
\usage{
read_bwg(
  files,
  format = c("auto", "bedgraph", "bigwig", "wig"),
  sample_names = NULL,
  strand = NULL,
  mode = c("lazy", "memory"),
  genome = NULL,
  check_chrom = TRUE,
  use_tabix = c("auto", "yes", "no"),
  tabix_empty_fallback = FALSE,
  verbose = TRUE
)
}
\arguments{
\item{files}{Signal file paths.}

\item{format}{Input format. Use auto, bedgraph, bigwig, or wig.}

\item{sample_names}{Optional sample names. If NULL, names are inferred from file basenames after removing compression suffixes and signal-track suffixes such as \code{.bedgraph.gz}, \code{.bigwig}, \code{.bw}, or \code{.wig}.}

\item{strand}{Optional strand labels for files.}

\item{mode}{lazy stores file paths, memory reads data into R.}

\item{genome}{Optional genome label.}

\item{check_chrom}{Whether to check chromosome names for in-memory files.}

\item{use_tabix}{Whether to use tabix/Rsamtools for indexed bedGraph-like files in lazy mode. Accepts \code{"auto"}, \code{"yes"}, \code{"no"}, TRUE, or FALSE. \code{TRUE} is equivalent to \code{"yes"}, and \code{FALSE} is equivalent to \code{"no"}.}

\item{tabix_empty_fallback}{Logical. Whether an empty tabix result should be verified by a full-file fread query. Default FALSE for performance. Set TRUE only when you suspect the tabix index coordinate convention is incompatible with the queried region.}

\item{verbose}{Logical. Whether to print read/setup progress messages.}
}
\value{
A BwgTrack object.
}
\description{
Read bedGraph, wig, or bigWig signal tracks
}
\details{
\code{format = "auto"} infers the input type from file extensions after removing
compression suffixes such as \code{.gz} or \code{.bgz}. \code{sample_names = NULL} infers
names from file basenames, removing suffixes such as \code{.bedgraph.gz}, \code{.bw},
\code{.bigwig}, and \code{.wig}.

For bigWig and wig files, strand information is not stored in the file, so
the sample is treated as unstranded. For paired plus/minus bedGraph files,
provide \code{strand = c("+", "-")} and matching \code{sample_names} if strand-specific
filtering is required.

\code{use_tabix = "auto"} uses indexed querying only when a \code{.tbi} index and an available backend are detected. GeneTrackR first checks the system \code{tabix} command and then the R package \code{Rsamtools}. Otherwise, lazy bedGraph queries fall back to full-file reading.

BigWig metadata, full-memory loading, and lazy regional queries use the
GeneTrackR native R BigWig reader. The compiled libBigWig reader is retained
temporarily for regression comparison but is no longer used by \code{read_bwg()}
or \code{retrieve_bwg()}.

\code{BwgTrack$seqinfo} follows the schema-v2 sequence metadata contract. bigWig
inputs record chromosome lengths from the file header. In-memory bedGraph and
wig inputs record observed chromosome names with unknown lengths (\code{NA}).
}
\examples{
\dontrun{
rnaseq_files <- system.file(
  "extdata",
  c("gtr_demo_rnaseq_plus.bedgraph", "gtr_demo_rnaseq_minus.bedgraph"),
  package = "GeneTrackR"
)
riboseq_files <- system.file(
  "extdata",
  c("gtr_demo_riboseq_plus.bedgraph", "gtr_demo_riboseq_minus.bedgraph"),
  package = "GeneTrackR"
)

rnaseq <- read_bwg(
  rnaseq_files,
  format = "bedgraph",
  sample_names = c("RNA_seq_plus", "RNA_seq_minus"),
  strand = c("+", "-"),
  mode = "memory"
)
riboseq <- read_bwg(
  riboseq_files,
  format = "bedgraph",
  sample_names = c("Ribo_seq_plus", "Ribo_seq_minus"),
  strand = c("+", "-"),
  mode = "memory"
)
}
}
