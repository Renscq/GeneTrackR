% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_tracks.R
\name{plot_tracks}
\alias{plot_tracks}
\title{Plot combined signal and gene model tracks}
\usage{
plot_tracks(
  annotation,
  signal = NULL,
  features = NULL,
  variants = NULL,
  chrom = NULL,
  start = NULL,
  end = NULL,
  gene_id = NULL,
  transcript_id = NULL,
  samples = NULL,
  sample_groups = NULL,
  signal_color_by = c("sample", "group"),
  signal_summary = c("none", "mean", "median", "sum"),
  signal_type = c("bar", "line", "area", "heatmap"),
  signal_palette = "Blues",
  signal_colors = NULL,
  gene_color_palette = "Paired",
  gene_fill_colors = NULL,
  gene_border_color = NA,
  signal_transform = c("none", "log2", "log10", "sqrt"),
  signal_y_scale = c("free", "fixed"),
  signal_y_ticks = c("range", "pretty"),
  grid_linewidth = 0.25,
  heatmap_bin_size = NULL,
  heatmap_max_bins = 800L,
  heatmap_summary = c("mean", "max", "sum", "median"),
  collapse = c("none", "union_exon", "longest"),
  strand = c("ignore", "+", "-", "both"),
  bin_size = NULL,
  highlight = NULL,
  layout = c("signal_top", "gene_top"),
  heights = c(signal = 3, gene = 1, feature = 0.8, variant = 0.7),
  cds_width = 0.5,
  utr_width = 0.25,
  show_direction = TRUE,
  direction_mode = c("transcript", "gene", "end"),
  label_position = c("axis", "feature", "none"),
  label_by = c("gene", "transcript"),
  label_side = c("above", "below", "center"),
  label_offset = 0.45,
  text_color = "black",
  text_size = 14,
  label_size = 12
)
}
\arguments{
\item{annotation}{A GenePred object or a standardized Feature object with transcript/exon records.}

\item{signal}{Optional BwgTrack object. If NULL, only annotation/feature/variant tracks are drawn.}

\item{features}{Optional FeatureTrack object or named list of FeatureTrack objects from \code{read_bed()}, \code{read_gff()}, or \code{read_gtf()}.}

\item{variants}{Optional VariantTrack object or named list of VariantTrack objects from \code{read_vcf_track()}.}

\item{chrom}{Chromosome name. Required when \code{gene_id} and \code{transcript_id} are not supplied.}

\item{start}{Region start in 1-based closed coordinates. Required with \code{chrom}/\code{end}.}

\item{end}{Region end in 1-based closed coordinates. Required with \code{chrom}/\code{start}.}

\item{gene_id}{Optional gene ID. If supplied, the plotting interval is inferred from the gene locus.}

\item{transcript_id}{Optional transcript ID. If supplied, the plotting interval is inferred from the transcript locus.}

\item{samples}{Optional signal sample IDs.}

\item{sample_groups}{Optional sample group mapping for group-level coloring or replicate summaries. Use a named character vector, a data frame with \code{sample_id} and \code{group}, or an unnamed vector with one group per selected sample.}

\item{signal_color_by}{Color signal tracks by \code{sample} or \code{group}.}

\item{signal_summary}{Replicate summary mode. Use \code{none} to plot individual samples, or \code{mean}, \code{median}, or \code{sum} to summarize samples within each group.}

\item{signal_type}{Signal plot type.}

\item{signal_palette}{Signal color palette for signal tracks. Any palette name from \code{RColorBrewer::brewer.pal.info} can be used.}

\item{signal_colors}{Optional named or unnamed vector of colors for signal samples.}

\item{gene_color_palette}{RColorBrewer palette name used for gene model feature fills.}

\item{gene_fill_colors}{Optional custom fill colors for gene model features. Use a named vector such as \code{c(CDS = "#33a02c", UTR = "#b2df8a", exon = "#fb9a99")}.}

\item{gene_border_color}{Optional rectangle border color for gene model features. Use \code{NA} to hide borders.}

\item{signal_transform}{Signal-axis transformation. Use \code{none}, \code{log2}, \code{log10}, or \code{sqrt}.}

\item{signal_y_scale}{Signal y-axis scale mode. Use \code{free} for independent sample-specific y-axis ranges or \code{fixed} for a shared y-axis range across samples.}

\item{signal_y_ticks}{Signal y-axis tick mode. Use \code{range} to show only integer minimum and maximum limits or \code{pretty} for default-style breaks.}

\item{grid_linewidth}{Grid line width in the signal panel.}

\item{collapse}{Gene model collapse mode for region-level plotting.}

\item{strand}{Signal strand selector.}

\item{bin_size}{Optional signal bin size.}

\item{highlight}{Optional data frame used to shade intervals on signal and gene model tracks. It must contain \code{start} and \code{end} columns in genomic coordinates. Optional columns are allowed but ignored by the default renderer.}

\item{layout}{Track layout. Use \code{signal_top} to place signal above gene model, or \code{gene_top} to place gene model above signal.}

\item{heights}{Relative panel heights. Must contain at least \code{signal}, \code{gene}, \code{feature}, and \code{variant} names when those tracks are used.}

\item{cds_width}{Vertical thickness of CDS rectangles in the gene model track.}

\item{utr_width}{Vertical thickness of UTR/non-coding exon rectangles in the gene model track.}

\item{show_direction}{Whether to draw direction arrows in the gene model track.}

\item{direction_mode}{Direction-arrow style for the gene model track. \code{transcript} draws one arrow per transcript, \code{gene} draws one arrow per gene, and \code{end} draws one short arrow at the directional end of each gene.}

\item{label_position}{Where to draw gene model labels. \code{axis} draws labels on the y axis, \code{feature} draws labels near the feature, and \code{none} hides labels.}

\item{label_by}{Which identifier to use for gene model labels. Use \code{gene} for gene IDs or \code{transcript} for transcript IDs.}

\item{label_side}{Label placement when \code{label_position = "feature"}. Use \code{above}, \code{below}, or \code{center}.}

\item{label_offset}{Vertical offset used for feature labels when \code{label_side} is \code{above} or \code{below}.}

\item{text_size}{Text size in points for axis text, axis titles, legends, and facet labels.}

\item{label_size}{Text size in points for gene model labels drawn when \code{label_position = "feature"}.}
}
\value{
A patchwork object or ggplot object.
}
\description{
Draw a genome-browser-like figure containing a gene model track alone or a
signal track combined with a gene model track. The genomic interval can be
specified in three ways: by \code{gene_id}, by \code{transcript_id}, or by explicit
\code{chrom}, \code{start}, and \code{end} coordinates.
}
\examples{
\dontrun{
gp <- read_genepred(
  system.file("extdata", "example.genePredExt", package = "GeneTrackR"),
  format = "genePredExt"
)
bg <- read_bwg(
  system.file("extdata", c("example_signal_A.bedgraph", "example_signal_B.bedgraph"), package = "GeneTrackR"),
  format = "bedgraph"
)

plot_tracks(annotation = gp, gene_id = "GeneA")
plot_tracks(annotation = gp, signal = bg, gene_id = "GeneA")
plot_tracks(annotation = gp, signal = bg, transcript_id = "TxA1")
plot_tracks(annotation = gp, signal = bg, chrom = "chr1", start = 1, end = 1200)
peaks <- read_bed(system.file("extdata", "example_features.bed", package = "GeneTrackR"))
vars <- read_vcf_track(system.file("extdata", "example_variants.vcf", package = "GeneTrackR"))
plot_tracks(annotation = gp, signal = bg, features = peaks, variants = vars, chrom = "chr1", start = 1, end = 1200)
}
}
