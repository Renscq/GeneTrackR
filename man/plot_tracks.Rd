% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_tracks.R
\name{plot_tracks}
\alias{plot_tracks}
\title{Plot combined signal and gene model tracks}
\usage{
plot_tracks(
  annotation,
  signal = NULL,
  features = NULL,
  variants = NULL,
  chrom = NULL,
  start = NULL,
  end = NULL,
  gene_id = NULL,
  transcript_id = NULL,
  samples = NULL,
  sample_groups = NULL,
  signal_color_by = c("sample", "group"),
  signal_summary = c("none", "mean", "median", "sum"),
  signal_type = c("bar", "line", "heatmap"),
  signal_palette = "Paired",
  signal_palette_direction = 1,
  signal_colors = NULL,
  gene_palette = "Paired",
  gene_colors = NULL,
  gene_border_color = NA,
  feature_color_by = "auto",
  feature_palette = "Paired",
  feature_colors = NULL,
  feature_border_color = NA,
  feature_max_legend_levels = 5,
  variant_palette = "Paired",
  variant_colors = NULL,
  ribo_signal_type = c("auto", "bar", "frame"),
  frame_palette = "Paired",
  frame_colors = NULL,
  signal_transform = c("none", "log2", "log10", "sqrt"),
  signal_y_scale = c("free", "fixed"),
  signal_y_ticks = c("range", "pretty"),
  heatmap_bin_size = NULL,
  heatmap_max_bins = 800L,
  heatmap_summary = c("mean", "max", "sum", "median"),
  collapse = c("none", "union_exon", "longest"),
  strand = c("ignore", "+", "-", "both"),
  bin_size = NULL,
  highlight = NULL,
  layout = c("signal_top", "gene_top"),
  heights = c(signal = 3, gene = 1, feature = 0.8, variant = 0.7),
  cds_height = 0.5,
  utr_height = 0.25,
  direction_mode = c("transcript", "gene", "end", "none"),
  label_position = c("axis", "feature"),
  label_by = c("gene", "transcript"),
  text_size = 14,
  signal_y_limits = NULL,
  signal_alpha = 0.85,
  signal_bar_width = 1,
  plot_theme = c("bw", "classic", "light", "minimal"),
  show_panel_border = NULL
)
}
\arguments{
\item{annotation}{A GenePred object or a standardized Feature object with transcript/exon records.}

\item{signal}{Optional BwgTrack object. If NULL, only annotation/feature/variant tracks are drawn.}

\item{features}{Optional FeatureTrack object or named list of FeatureTrack objects from \code{read_bed()}, \code{read_gff()}, or \code{read_gtf()}.}

\item{variants}{Optional VariantTrack object or named list of VariantTrack objects from \code{read_vcf()}.}

\item{chrom}{Chromosome name. Required when \code{gene_id} and \code{transcript_id} are not supplied.}

\item{start}{Region start in 1-based closed coordinates. Required with \code{chrom}/\code{end}.}

\item{end}{Region end in 1-based closed coordinates. Required with \code{chrom}/\code{start}.}

\item{gene_id}{Optional gene ID. If supplied, the plotting interval is inferred from the gene locus.}

\item{transcript_id}{Optional transcript ID. If supplied, the plotting interval is inferred from the transcript locus.}

\item{samples}{Optional signal sample IDs.}

\item{sample_groups}{Optional sample group mapping for group-level coloring or replicate summaries. Use a named character vector, a data frame with \code{sample_id} and \code{group}, or an unnamed vector with one group per selected sample.}

\item{signal_color_by}{Color signal tracks by \code{sample} or \code{group}.}

\item{signal_summary}{Replicate summary mode. Use \code{none} to plot individual samples, or \code{mean}, \code{median}, or \code{sum} to summarize samples within each group.}

\item{signal_type}{Signal plot type for non-Ribo-seq browser signal tracks.}

\item{signal_palette}{Signal color palette for signal tracks. Any palette name from \code{RColorBrewer::brewer.pal.info} can be used. Discrete signal sample/group colors follow the standard RColorBrewer class order; heatmaps use the corresponding continuous gradient.}

\item{signal_palette_direction}{Direction for generated signal colors. Use \code{1} for the standard palette order and \code{-1} for the reversed palette order. Discrete sample/group colors preserve level-to-color order; heatmap gradients reverse continuously.}

\item{signal_colors}{Optional named or unnamed vector of explicit colors for signal samples. Explicit colors override \code{signal_palette} and are not modified by \code{signal_palette_direction}.}

\item{gene_palette}{RColorBrewer palette name used for gene model feature fills.}

\item{gene_colors}{Optional custom fill colors for gene model features. Use a named vector such as \code{c(UTR = "#b2df8a", CDS = "#33a02c", exon = "#fb9a99")}.}

\item{gene_border_color}{Optional rectangle border color for gene model features. Use \code{NA} to hide borders.}

\item{feature_color_by}{Feature-track color grouping. \code{auto} chooses a compact grouping automatically; other common choices are \code{feature_group}, \code{type}, \code{source}, \code{name}, and \code{strand}.}

\item{feature_palette}{RColorBrewer palette name used for feature-track fills.}

\item{feature_colors}{Optional explicit feature-track fill colors.}

\item{feature_border_color}{Optional rectangle border color for feature tracks. Use \code{NA} to hide borders.}

\item{feature_max_legend_levels}{Maximum number of legend groups shown for each feature track.}

\item{variant_palette}{RColorBrewer palette name used for variant-track colors.}

\item{variant_colors}{Optional named or unnamed explicit colors passed to \code{plot_variant()}. Named vectors are recommended when stable SNP/INS/DEL colors are required.}

\item{ribo_signal_type}{How Ribo-seq samples should be displayed in transcript-centered browser plots. \code{auto} uses \code{frame} for samples whose IDs look like Ribo-seq/RPF tracks when \code{transcript_id} is supplied, otherwise it falls back to standard genomic signal tracks. \code{bar} always uses the standard signal geometry, and \code{frame} forces frame rendering when transcript-centered plotting is possible.}

\item{frame_palette}{RColorBrewer palette name used for Ribo-seq frame plots.}

\item{frame_colors}{Optional explicit colors for \code{frame0}, \code{frame1}, and \code{frame2}.}

\item{signal_transform}{Signal-axis transformation. Use \code{none}, \code{log2}, \code{log10}, or \code{sqrt}.}

\item{signal_y_scale}{Signal y-axis scale mode. Use \code{free} for independent sample-specific y-axis ranges or \code{fixed} for a shared y-axis range across samples.}

\item{signal_y_ticks}{Signal y-axis tick mode. Use \code{range} to show only integer minimum and maximum limits or \code{pretty} for default-style breaks.}

\item{heatmap_bin_size}{Optional bin size used only for heatmap rendering.
\code{NULL} uses adaptive binning.}

\item{heatmap_max_bins}{Maximum number of heatmap bins when
\code{heatmap_bin_size = NULL}.}

\item{heatmap_summary}{Summary statistic used to aggregate signal into
heatmap bins: \code{mean}, \code{max}, \code{sum}, or \code{median}.}

\item{collapse}{Gene model collapse mode for region-level plotting.}

\item{strand}{Signal strand selector.}

\item{bin_size}{Optional signal bin size.}

\item{highlight}{Optional data frame used to shade intervals on signal and gene model tracks. It must contain \code{start} and \code{end} columns in genomic coordinates. Optional columns are allowed but ignored by the default renderer.}

\item{layout}{Track layout. Use \code{signal_top} to place signal above gene model, or \code{gene_top} to place gene model above signal.}

\item{heights}{Relative panel heights. Must contain at least \code{signal}, \code{gene}, \code{feature}, and \code{variant} names when those tracks are used.}

\item{cds_height}{Vertical thickness of CDS rectangles in the gene model track.}

\item{utr_height}{Vertical thickness of UTR/non-coding exon rectangles in the gene model track.}

\item{direction_mode}{Direction-arrow style for the gene model track. \code{transcript} draws one arrow per transcript, \code{gene} draws one arrow per gene, \code{end} draws one short arrow at the directional end of each gene, and \code{none} hides direction arrows.}

\item{label_position}{Where to draw gene model labels. \code{axis} draws labels on the y axis and \code{feature} draws labels near the feature.}

\item{label_by}{Which identifier to use for gene model labels. Use \code{gene} for gene IDs or \code{transcript} for transcript IDs.}

\item{text_size}{Text size in points for axis text, axis titles, legends, and facet labels.}

\item{signal_y_limits}{Optional two-element numeric vector giving the plotted y-axis limits after \code{signal_transform}. Supplying limits changes \code{signal_y_scale} to \code{fixed}.}

\item{signal_alpha}{Signal geometry transparency from 0 to 1.}

\item{signal_bar_width}{Relative width of bar intervals from greater than 0 to 1. A value below 1 creates proportional gaps without changing interval centers.}

\item{plot_theme}{Base ggplot2 theme used by all standard track panels. Use \code{bw}, \code{classic}, \code{light}, or \code{minimal}.}

\item{show_panel_border}{Whether to draw panel borders. \code{NULL} preserves the selected theme default.}
}
\value{
A patchwork object or ggplot object.
}
\description{
Draw a genome-browser-like figure containing a gene model track alone or a
signal track combined with gene, feature, and variant tracks. The genomic
interval can be specified in three ways: by \code{gene_id}, by \code{transcript_id},
or by explicit \code{chrom}, \code{start}, and \code{end} coordinates.
}
\examples{
gp_file <- system.file(
  "extdata", "gtr_demo.genePredExt", package = "GeneTrackR"
)
gp <- read_genepred(
  gp_file, format = "genePredExt", verbose = FALSE, progress = FALSE
)
plot_tracks(annotation = gp, gene_id = "GeneA")
}
