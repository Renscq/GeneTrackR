% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/retrieve_bwg.R
\name{retrieve_bwg}
\alias{retrieve_bwg}
\title{Retrieve signal records from a BwgTrack object}
\usage{
retrieve_bwg(
  object,
  chrom,
  start,
  end,
  samples = NULL,
  strand = c("ignore", "+", "-", "both", "auto"),
  strand_policy = c("ignore_unstranded", "strict"),
  as = c("data.table", "BwgTrack", "GRanges"),
  verbose = FALSE,
  progress = interactive() && isTRUE(verbose),
  keep_empty_samples = FALSE,
  tabix_empty_fallback = NULL
)
}
\arguments{
\item{object}{A BwgTrack object returned by \code{read_bwg()}.}

\item{chrom}{Chromosome name.}

\item{start}{Region start in 1-based closed coordinates.}

\item{end}{Region end in 1-based closed coordinates.}

\item{samples}{Optional character vector of sample IDs to retrieve. If NULL,
all samples are queried.}

\item{strand}{Strand selector. Use \code{"ignore"} for unstranded retrieval,
\code{"+"} or \code{"-"} for strand-specific tracks, \code{"both"} for both strands, or
\code{"auto"} when called by higher-level gene-aware plotting functions.}

\item{strand_policy}{How to handle strand filtering for unstranded signal
files. \code{"ignore_unstranded"} returns unstranded tracks for any strand request;
\code{"strict"} only returns explicitly matching strand records.}

\item{as}{Output type. \code{"data.table"} returns a signal table, \code{"BwgTrack"}
returns an in-memory BwgTrack sub-object, and \code{"GRanges"} returns a GRanges
object.}

\item{verbose}{Logical. Whether to print progress messages.}

\item{progress}{Logical. Whether to show a text progress bar for multi-sample
queries.}

\item{keep_empty_samples}{Logical. If TRUE, samples without signal in the
requested region are returned as zero-valued placeholder intervals. This is
useful for preserving empty facets in plots.}

\item{tabix_empty_fallback}{Optional logical. If TRUE, empty tabix results are
verified by full-file fread. Keep FALSE for speed unless debugging tabix
coordinate/index issues.}
}
\value{
A data.table, BwgTrack object, or GRanges object.
}
\description{
\code{retrieve_bwg()} is the unified signal retrieval API in GeneTrackR. It
retrieves signal intervals from bedGraph, wig, or bigWig tracks by genomic
region. It replaces the older \code{query_bwg()} interface.
}
\examples{
\dontrun{
bg <- read_bwg("sample.bedgraph.gz", format = "bedgraph", mode = "lazy")

dt <- retrieve_bwg(
  bg,
  chrom = "chr1",
  start = 1,
  end = 10000
)

bg_sub <- retrieve_bwg(
  bg,
  chrom = "chr1",
  start = 1,
  end = 10000,
  as = "BwgTrack"
)
}

}
