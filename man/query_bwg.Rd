% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bwg_ops.R
\name{query_bwg}
\alias{query_bwg}
\title{Query signal values by genomic region}
\usage{
query_bwg(
  object,
  chrom,
  start,
  end,
  samples = NULL,
  strand = c("ignore", "+", "-", "both", "auto"),
  strand_policy = c("ignore_unstranded", "strict"),
  verbose = FALSE,
  progress = interactive() && isTRUE(verbose),
  keep_empty_samples = FALSE,
  tabix_empty_fallback = NULL
)
}
\arguments{
\item{object}{A BwgTrack object.}

\item{chrom}{Chromosome name.}

\item{start}{Region start in 1-based closed coordinates.}

\item{end}{Region end in 1-based closed coordinates.}

\item{samples}{Optional sample IDs.}

\item{strand}{Strand selector: ignore, +, -, both, or auto.}

\item{strand_policy}{How to handle strand filtering for unstranded signal files. Use \code{ignore_unstranded} to ignore strand filters for bigWig/wig-like unstranded signals, or \code{strict} to return only records with matching explicit strand information.}

\item{verbose}{Logical. Whether to print query progress messages.}

\item{progress}{Logical. Whether to show a text progress bar for multi-sample queries.}

\item{keep_empty_samples}{Logical. If TRUE, samples without any signal interval in the queried region are returned as zero-valued placeholder intervals. This is mainly useful for keeping empty tracks visible in downstream plots.}

\item{tabix_empty_fallback}{Optional logical. If TRUE, an empty tabix result is verified by a full-file fread query. The default NULL uses the setting stored in the BwgTrack object. For speed, keep this FALSE unless debugging tabix coordinate issues.}
}
\value{
A data.table with signal records.
}
\description{
Query signal values by genomic region
}
\details{
\code{samples} can be used to query only a subset of samples. For unstranded
bigWig/wig signals, \code{strand = "+"} or \code{"-"} has no biological filtering
meaning unless \code{strand_policy = "strict"} is used. With the default
\code{strand_policy = "ignore_unstranded"}, unstranded tracks are returned for
either strand request. For strand-specific bedGraph files created with
\code{read_bwg(strand = c("+", "-"))}, \code{strand_policy = "strict"} keeps only
matching strand records. For strand-specific bedGraph files, samples with the
opposite strand are skipped before file querying, which avoids unnecessary
reading of plus/minus paired tracks.
}
\examples{
\dontrun{
query_bwg(bg, chrom = "chr1", start = 1, end = 1000)
query_bwg(bg, chrom = "chr1", start = 1, end = 1000, samples = "sampleA")
query_bwg(
  bg,
  chrom = "chr1",
  start = 1,
  end = 1000,
  strand = "+",
  strand_policy = "strict",
  verbose = TRUE,
  progress = TRUE
)
}
}
