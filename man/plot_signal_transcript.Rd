% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_signal.R
\name{plot_signal_transcript}
\alias{plot_signal_transcript}
\title{Plot signal over a transcript}
\usage{
plot_signal_transcript(
  signal,
  annotation,
  transcript_id,
  samples = NULL,
  sample_groups = NULL,
  signal_color_by = c("sample", "group"),
  signal_summary = c("none", "mean", "median", "sum"),
  coordinate = c("transcript", "genomic"),
  plot_type = c("bar", "line", "heatmap", "frame"),
  strand = c("auto", "+", "-", "both", "ignore"),
  bin_size = NULL,
  highlight = NULL,
  show_gene_model = TRUE,
  signal_track_height = 3,
  gene_track_height = 1,
  signal_palette = "Blues",
  signal_palette_direction = 1,
  signal_colors = NULL,
  frame_palette = "Set1",
  frame_colors = NULL,
  signal_transform = c("none", "log2", "log10", "sqrt"),
  signal_y_scale = c("free", "fixed"),
  signal_y_ticks = c("range", "pretty"),
  heatmap_bin_size = NULL,
  heatmap_max_bins = 800L,
  heatmap_summary = c("mean", "max", "sum", "median"),
  cds_height = 0.5,
  utr_height = 0.25,
  direction_mode = c("transcript", "gene", "end", "none"),
  label_position = c("axis", "feature"),
  label_by = c("gene", "transcript"),
  text_size = 14,
  signal_y_limits = NULL,
  signal_alpha = 0.85,
  signal_bar_width = 1,
  plot_theme = c("bw", "classic", "light", "minimal"),
  show_panel_border = NULL
)
}
\arguments{
\item{signal}{A BwgTrack object.}

\item{annotation}{A GenePred object.}

\item{transcript_id}{Transcript ID.}

\item{samples}{Optional sample IDs to plot. If NULL, all samples are used.}

\item{sample_groups}{Optional sample group mapping for group-level coloring or replicate summaries. Use a named character vector, a data frame with \code{sample_id} and \code{group}, or an unnamed vector with one group per selected sample.}

\item{signal_color_by}{Color signal tracks by \code{sample} or \code{group}.}

\item{signal_summary}{Replicate summary mode. Use \code{none} to plot individual samples, or \code{mean}, \code{median}, or \code{sum} to summarize samples within each group. Summary is performed on the current signal intervals, so using \code{bin_size} is recommended when raw interval boundaries differ among samples.}

\item{coordinate}{Coordinate mode. \code{transcript} removes introns and displays spliced transcript coordinates, while \code{genomic} keeps genomic coordinates.}

\item{plot_type}{Signal plot type: \code{bar}, \code{line}, \code{heatmap}, or \code{frame}. \code{frame} is designed for Ribo-seq style transcript plots and colors CDS positions by frame0/frame1/frame2.}

\item{strand}{Strand selector. Use \code{auto} to use the transcript strand.}

\item{bin_size}{Optional bin size for signal aggregation.}

\item{highlight}{Optional data frame used to shade intervals on the signal and gene model tracks. It must contain \code{start} and \code{end} columns. For \code{coordinate = "genomic"}, these are genomic coordinates; for \code{coordinate = "transcript"}, these are spliced transcript coordinates.}

\item{show_gene_model}{Whether to append the transcript gene model track. Default TRUE.}

\item{signal_track_height}{Relative height of the signal panel when the gene model is shown. Default 3.}

\item{gene_track_height}{Relative height of the gene model panel when it is shown. Default 1.}

\item{signal_palette}{Signal color palette. Any palette name from \code{RColorBrewer::brewer.pal.info} can be used, such as \code{Blues}, \code{Reds}, \code{RdBu}, \code{Paired}, \code{Set1}, \code{Dark2}, \code{YlGnBu}, or \code{Spectral}. Discrete sample/group colors are assigned in the standard RColorBrewer class order; heatmaps use the corresponding continuous gradient.}

\item{signal_palette_direction}{Direction for generated signal colors. Use \code{1} for the standard palette order and \code{-1} for the reversed palette order. Discrete sample/group colors preserve level-to-color order; heatmap gradients reverse continuously.}

\item{signal_colors}{Optional named or unnamed vector of explicit colors for samples. If supplied, it overrides \code{signal_palette}; explicit colors are not modified by \code{signal_palette_direction}.}

\item{frame_palette}{RColorBrewer palette for CDS frame colors when \code{plot_type = "frame"}. Default is \code{Set1}. Colors are assigned in order to \code{frame0}, \code{frame1}, and \code{frame2}.}

\item{frame_colors}{Optional named colors for \code{frame0}, \code{frame1}, and \code{frame2}.}

\item{signal_transform}{Signal-axis transformation. Use \code{none}, \code{log2}, \code{log10}, or \code{sqrt}. Log transforms use signed log1p-style transformation to tolerate zero values.}

\item{signal_y_scale}{Signal y-axis scale mode. Use \code{free} for each sample to have its own y-axis range, or \code{fixed} to force all samples to share the same y-axis range.}

\item{signal_y_ticks}{Signal y-axis tick mode. Use \code{range} to show only integer axis limits as the minimum and maximum ticks, or \code{pretty} to use ggplot2 default-style breaks.}

\item{cds_height}{Vertical thickness of CDS rectangles in the gene model track.}

\item{utr_height}{Vertical thickness of UTR/non-coding exon rectangles in the gene model track.}

\item{direction_mode}{Direction-arrow style for the gene model track. \code{transcript} draws one arrow per transcript, \code{gene} draws one arrow per gene, \code{end} draws one short arrow at the directional end of each gene, and \code{none} hides direction arrows.}

\item{label_position}{Where to draw gene model labels. \code{axis} draws labels on the y axis and \code{feature} draws labels on the model.}

\item{label_by}{Which identifier to use for gene model labels.}

\item{text_size}{Text size in points for signal and gene model axis text, axis titles, facet strips, and legends.}

\item{signal_y_limits}{Optional two-element numeric vector giving the plotted y-axis limits after \code{signal_transform}. Supplying limits changes \code{signal_y_scale} to \code{fixed}.}

\item{signal_alpha}{Signal geometry transparency from 0 to 1.}

\item{signal_bar_width}{Relative width of bar intervals from greater than 0 to 1. A value below 1 creates proportional gaps without changing interval centers.}

\item{plot_theme}{Base ggplot2 theme. Use \code{bw}, \code{classic}, \code{light}, or \code{minimal}.}

\item{show_panel_border}{Whether to draw panel borders. \code{NULL} preserves the selected theme default.}
}
\value{
A ggplot or patchwork object.
}
\description{
Plot signal over a transcript
}
\details{
\code{samples} selects the samples to draw. \code{sample_groups} can be a named vector
or a data frame with \code{sample_id} and \code{group}. Use \code{signal_color_by = "group"}
to color by group, and \code{signal_summary = "mean"}, \code{"median"}, or \code{"sum"}
to collapse replicates within groups. \code{signal_colors} overrides
\code{signal_palette} and may be named by sample ID or group name. See also
\link{GeneTrackR-advanced-parameters}.
}
\examples{
\dontrun{
gp <- read_genepred(
  system.file("extdata", "gtr_demo.genePredExt", package = "GeneTrackR"),
  format = "genePredExt",
  verbose = FALSE
)
riboseq <- read_bwg(
  system.file(
    "extdata",
    c("gtr_demo_riboseq_plus.bedgraph", "gtr_demo_riboseq_minus.bedgraph"),
    package = "GeneTrackR"
  ),
  format = "bedgraph",
  sample_names = c("Ribo_seq_plus", "Ribo_seq_minus"),
  strand = c("+", "-"),
  mode = "memory"
)
plot_signal_transcript(
  signal = riboseq,
  annotation = gp,
  transcript_id = "TxA1",
  coordinate = "transcript",
  plot_type = "frame"
)
}
}
