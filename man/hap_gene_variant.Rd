% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hap_variant.R
\name{hap_gene_variant}
\alias{hap_gene_variant}
\title{Build haplotypes from variants in a gene or transcript region}
\usage{
hap_gene_variant(
  vcf,
  annotation,
  gene_id = NULL,
  transcript_id = NULL,
  upstream = 0L,
  downstream = 0L,
  strand_aware = TRUE,
  samples = NULL,
  variant_type = NULL,
  genotype_mode = c("code", "string"),
  missing_genotype = NA_character_,
  min_variant_number = NULL
)
}
\arguments{
\item{vcf}{A VariantTrack object or VCF file path.}

\item{annotation}{A gene annotation object used to locate \code{gene_id} or \code{transcript_id}.}

\item{gene_id}{Optional gene ID. Use exactly one of \code{gene_id} or \code{transcript_id}.}

\item{transcript_id}{Optional transcript ID. Use exactly one of \code{gene_id} or \code{transcript_id}.}

\item{upstream}{Upstream flanking length in bp. When \code{strand_aware = TRUE}, upstream is interpreted relative to the gene/transcript strand.}

\item{downstream}{Downstream flanking length in bp. When \code{strand_aware = TRUE}, downstream is interpreted relative to the gene/transcript strand.}

\item{strand_aware}{Logical. Whether upstream/downstream should follow strand direction. Default TRUE.}

\item{samples}{Optional sample names to keep.}

\item{variant_type}{Optional variant types to keep.}

\item{genotype_mode}{Genotype representation. \code{code} converts genotypes to compact 0/1 states, where 0 means reference genotype and 1 means any alternate allele is present. \code{string} converts genotypes to a single allele label; long InDel alleles are compressed as \code{iN}, where \code{N} is the displayed REF or ALT allele length, so the haplotype cell uses the same label as the corresponding REF/ALT row.}

\item{missing_genotype}{Missing genotype label. Default is \code{NA_character_}, which is displayed as \code{NA} in haplotype tables.}

\item{min_variant_number}{Minimum number of non-missing variants required for a sample. If NULL, only samples with complete non-missing genotypes across all retained variants are kept.}
}
\value{
A HapVariant object.
}
\description{
Extracts variants for a specified gene or transcript, optionally including
upstream and downstream flanking regions, and converts sample genotype
profiles into haplotypes.
}
\examples{
vcf_file <- system.file("extdata", "gtr_demo_variants.vcf", package = "GeneTrackR")
anno_file <- system.file("extdata", "gtr_demo.genePredExt", package = "GeneTrackR")
vcf <- read_vcf(vcf_file, mode = "memory", verbose = FALSE)
anno <- read_genepred(anno_file, format = "genePredExt", verbose = FALSE)
hap <- hap_gene_variant(
  vcf, annotation = anno, gene_id = "GeneA",
  upstream = 1000, downstream = 500, min_variant_number = 1
)
hap
hap$haplotypes
hap_tx <- hap_gene_variant(
  vcf, annotation = anno, transcript_id = "TxA1",
  genotype_mode = "string", min_variant_number = 1
)
hap_tx$haplotypes
}
