% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/as_granges.R
\name{as_granges}
\alias{as_granges}
\title{Convert a GeneTrackR genomic track object to GRanges}
\usage{
as_granges(object, level = c("gene", "transcript", "exon", "feature"))
}
\arguments{
\item{object}{A GenePred, Feature, FeatureTrack, BwgTrack, or VariantTrack object.}

\item{level}{Annotation feature level. One of \code{gene}, \code{transcript}, \code{exon},
or \code{feature}. Ignored for BwgTrack and VariantTrack objects.}
}
\value{
A \code{GenomicRanges::GRanges} object.
}
\description{
Converts GeneTrackR annotation, signal, and variant objects to
\code{GenomicRanges::GRanges} so they can be used directly with Bioconductor
genomic interval infrastructure. Annotation objects support gene,
transcript, exon, and feature levels. In-memory \code{BwgTrack} objects expose
\code{sample_id} and signal \code{score} metadata, while \code{VariantTrack} objects retain
their VCF-derived metadata columns. Lazy signal or variant objects should be
retrieved by genomic region before conversion.
}
\examples{
gp_file <- system.file("extdata", "gtr_demo.genePredExt", package = "GeneTrackR")
gp <- read_genepred(gp_file, format = "genePredExt", verbose = FALSE)
as_granges(gp, level = "gene")

signal <- BwgTrack(
  samples = data.frame(sample_id = "sampleA"),
  data = data.frame(
    sample_id = "sampleA", chrom = "chr1", start = 10L, end = 12L,
    value = 1.5, strand = "*"
  )
)
as_granges(signal)

vcf_file <- system.file("extdata", "gtr_demo_variants.vcf", package = "GeneTrackR")
vcf <- read_vcf(vcf_file, mode = "memory", verbose = FALSE)
as_granges(vcf)
}
