% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hap_variant.R
\name{hap_variant}
\alias{hap_variant}
\title{Build haplotypes from variants in a gene or genomic region}
\usage{
hap_variant(
  vcf,
  annotation = NULL,
  gene_id = NULL,
  transcript_id = NULL,
  chrom = NULL,
  start = NULL,
  end = NULL,
  samples = NULL,
  variant_type = NULL,
  genotype_mode = c("code", "string"),
  missing_genotype = NA_character_,
  min_variant_number = NULL
)
}
\arguments{
\item{vcf}{A VariantTrack object or VCF file path.}

\item{annotation}{Optional gene annotation object used for \code{gene_id} or \code{transcript_id} queries.}

\item{gene_id}{Optional gene ID.}

\item{transcript_id}{Optional transcript ID.}

\item{chrom}{Optional chromosome name.}

\item{start}{Optional region start in 1-based closed coordinates.}

\item{end}{Optional region end in 1-based closed coordinates.}

\item{samples}{Optional sample names to keep.}

\item{variant_type}{Optional variant types to keep.}

\item{genotype_mode}{Genotype representation. \code{code} converts genotypes to compact 0/1 states, where 0 means reference genotype and 1 means any alternate allele is present. \code{string} converts genotypes to a single allele label; long InDel alleles are compressed as \code{iN}, where \code{N} is allele length.}

\item{missing_genotype}{Missing genotype label. Default is \code{NA_character_}, which is displayed as \code{NA} in haplotype tables.}

\item{min_variant_number}{Minimum number of non-missing variants required for a sample. If NULL, only samples with complete non-missing genotypes across all retained variants are kept.}
}
\value{
A HapVariant object.
}
\description{
Extracts variants from a VCF/VariantTrack object and converts sample genotype
profiles into haplotypes. The function supports region-based queries and
annotation-guided gene/transcript queries.
}
\examples{
vcf_file <- system.file("extdata", "example_haplotype.vcf", package = "GeneTrackR")
anno_file <- system.file("extdata", "example.genePredExt", package = "GeneTrackR")
vcf <- read_vcf(vcf_file)
anno <- read_genepred(anno_file, format = "genePredExt", verbose = FALSE)
hap <- hap_variant(vcf, annotation = anno, gene_id = "GeneA", genotype_mode = "code")
hap
hap$haplotypes
hap_string <- hap_variant(vcf, annotation = anno, gene_id = "GeneA", genotype_mode = "string")
hap_string$haplotypes
}
