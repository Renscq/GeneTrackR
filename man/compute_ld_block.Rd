% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compute_ld_block.R
\name{compute_ld_block}
\alias{compute_ld_block}
\title{Compute a linkage disequilibrium block from VCF genotypes}
\usage{
compute_ld_block(
  vcf,
  chrom = NULL,
  start = NULL,
  end = NULL,
  variant_type = c("both", "snp", "ind"),
  method = c("r2", "Dprime"),
  samples = NULL,
  min_pair_samples = 3L,
  ploidy = 2L,
  keep_genotype_matrix = FALSE,
  verbose = TRUE
)
}
\arguments{
\item{vcf}{A \code{VariantTrack} object returned by \code{read_vcf()} or a VCF path.}

\item{chrom}{Chromosome. If NULL, a single-chromosome in-memory VCF uses its
full chromosome range.}

\item{start}{Region start. If NULL, the minimum retained variant position is used.}

\item{end}{Region end. If NULL, the maximum retained variant position is used.}

\item{variant_type}{One of \code{both}, \code{snp}, or \code{ind}. \code{ind} keeps INS/DEL/MNV
records.}

\item{method}{LD value used as the primary \code{ld} column. Currently \code{r2} and
\code{Dprime} are supported. Dprime is a dosage-based approximation for unphased VCFs.}

\item{samples}{Optional sample names to keep.}

\item{min_pair_samples}{Minimum number of paired non-missing samples required
for a pairwise estimate.}

\item{ploidy}{Ploidy used for dosage-based allele-frequency and Dprime
approximation. Default is 2.}

\item{keep_genotype_matrix}{Logical. Whether to store the variant-by-sample
dosage matrix in the returned object.}

\item{verbose}{Logical. Whether to print progress messages.}
}
\value{
An \code{LDTrack} object.
}
\description{
Calculates pairwise LD for variants in a genomic region. Genotypes are parsed
from the sample columns preserved by \code{read_vcf(..., keep_genotype = TRUE)}.
The current implementation uses genotype dosage, where 0 is homozygous
reference, 1 is heterozygous/non-reference carrier, and 2 is homozygous
alternate for diploid biallelic records. Multi-allelic non-reference alleles
are collapsed into total non-reference dosage.
}
\examples{
vcf_file <- system.file("extdata", "gtr_demo_variants.vcf", package = "GeneTrackR")
vcf <- read_vcf(vcf_file, mode = "memory", verbose = FALSE)
ld <- compute_ld_block(
  vcf,
  chrom = "chr1",
  start = 12342620,
  end = 12355500,
  variant_type = "snp",
  method = "r2",
  verbose = FALSE
)
ld$data
}
