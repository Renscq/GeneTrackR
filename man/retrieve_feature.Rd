% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/zzz_retrieve_merge.R
\name{retrieve_feature}
\alias{retrieve_feature}
\title{Retrieve annotation features from a Feature-compatible object}
\usage{
retrieve_feature(
  object,
  pattern = NULL,
  level = c("feature", "gene", "transcript", "exon"),
  chrom = NULL,
  start = NULL,
  end = NULL,
  mode = c("overlap", "within"),
  gene_id = NULL,
  transcript_id = NULL,
  type = NULL,
  ignore_case = TRUE,
  fixed = FALSE
)
}
\arguments{
\item{object}{A Feature-compatible annotation object.}

\item{pattern}{Optional character pattern to search IDs/names.}

\item{level}{Retrieval level. Use \code{feature}, \code{gene}, \code{transcript}, or \code{exon}.}

\item{chrom}{Optional chromosome filter.}

\item{start}{Optional region start.}

\item{end}{Optional region end.}

\item{mode}{Region selection mode. \code{overlap} keeps overlapping records and
\code{within} keeps records fully contained in the region.}

\item{gene_id}{Optional gene ID filter.}

\item{transcript_id}{Optional transcript ID filter.}

\item{type}{Optional feature type filter. Only used for \code{level = "feature"}.}

\item{ignore_case}{Logical. Whether pattern matching ignores case.}

\item{fixed}{Logical. Whether \code{pattern} should be treated as a fixed string.}
}
\value{
A data.table containing retrieved records.
}
\description{
\code{retrieve_feature()} is the unified retrieval API for annotation objects.
It replaces the older \code{search_feature()}, \code{search_gene()}, and
GenePred-specific search helpers. It can retrieve standardized feature rows,
gene rows, transcript rows, or exon rows from GenePred/GTF/GFF/BED-derived
Feature objects.
}
\examples{
\dontrun{
retrieve_feature(gp, pattern = "GeneA")
retrieve_feature(gp, level = "gene", pattern = "GeneA")
retrieve_feature(gp, level = "transcript", gene_id = "GeneA")
retrieve_feature(gp, chrom = "chr1", start = 1, end = 1000)
}
}
