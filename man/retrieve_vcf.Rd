% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/retrieve_vcf.R
\name{retrieve_vcf}
\alias{retrieve_vcf}
\title{Retrieve variants from a VariantTrack or an indexed VCF file}
\usage{
retrieve_vcf(
  object,
  pattern = NULL,
  chrom = NULL,
  start = NULL,
  end = NULL,
  annotation = NULL,
  gene_id = NULL,
  transcript_id = NULL,
  upstream = 0L,
  downstream = 0L,
  strand_aware = TRUE,
  variant_id = NULL,
  variant_type = NULL,
  keep_genotype = TRUE,
  ignore_case = TRUE,
  fixed = FALSE,
  as = c("data.table", "VariantTrack"),
  verbose = TRUE,
  progress = interactive() && isTRUE(verbose)
)
}
\arguments{
\item{object}{A VariantTrack object or a path to a VCF/VCF.GZ file.}

\item{pattern}{Optional text pattern matched against variant ID, REF, ALT, INFO, and variant type.}

\item{chrom}{Optional chromosome name. Required for indexed file region queries unless \code{gene_id} or \code{transcript_id} is supplied.}

\item{start}{Optional 1-based region start.}

\item{end}{Optional 1-based region end.}

\item{annotation}{Optional GenePred/Feature annotation object used for gene/transcript-aware retrieval.}

\item{gene_id}{Optional gene ID. When supplied, \code{annotation} is used to resolve the gene range.}

\item{transcript_id}{Optional transcript ID. When supplied, \code{annotation} is used to resolve the transcript range.}

\item{upstream}{Upstream flanking length in bp for gene/transcript queries.}

\item{downstream}{Downstream flanking length in bp for gene/transcript queries.}

\item{strand_aware}{Logical. Whether upstream/downstream should follow gene/transcript strand direction.}

\item{variant_id}{Optional variant ID vector.}

\item{variant_type}{Optional variant type vector, such as \code{SNP}, \code{INS}, \code{DEL}, or \code{MNV}.}

\item{keep_genotype}{Logical. Whether to keep FORMAT and sample genotype columns when \code{object} is a VCF file path.}

\item{ignore_case}{Logical. Whether pattern matching ignores case.}

\item{fixed}{Logical. Whether pattern is matched as a fixed string.}

\item{as}{Output type: \code{data.table} or \code{VariantTrack}.}

\item{verbose}{Logical. Whether to print progress messages.}

\item{progress}{Logical. Whether to print a compact stage-level progress indicator.}
}
\value{
A data.table or VariantTrack object.
}
\description{
Retrieves VCF records by genomic region, variant ID, type, or text pattern.
For large bgzip-compressed VCF files with a tabix index, \code{object} can be a
file path and the function will query only the requested region using
\code{Rsamtools::scanTabix()}.
}
\examples{
vcf_file <- system.file("extdata", "gtr_demo_variants.vcf", package = "GeneTrackR")
vcf <- read_vcf(vcf_file, mode = "memory", verbose = FALSE)
retrieve_vcf(vcf, chrom = "chr1", start = 12339700, end = 12343200)
retrieve_vcf(vcf, variant_type = "SNP")
retrieve_vcf(vcf, pattern = "varA", fixed = TRUE)
vt <- retrieve_vcf(vcf, chrom = "chr1", start = 12339700, end = 12343200, as = "VariantTrack")
vt
}
