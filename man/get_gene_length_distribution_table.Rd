% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_gene_length_distribution.R
\name{get_gene_length_distribution_table}
\alias{get_gene_length_distribution_table}
\alias{get_genepred_length_table}
\title{Extract length records from a gene annotation object}
\usage{
get_gene_length_distribution_table(
  object,
  feature = c("all", "gene", "transcript", "exon", "cds", "utr", "five_utr", "three_utr"),
  unit = c("auto", "transcript", "segment"),
  transcript_length = c("spliced", "genomic"),
  chrom = NULL,
  start = NULL,
  end = NULL,
  mode = c("overlap", "within", "trim"),
  keep_zero = FALSE
)

get_genepred_length_table(...)
}
\arguments{
\item{object}{A Feature or GenePred-compatible annotation object.}

\item{feature}{Feature type to extract. Use \code{all} to return gene, transcript,
exon, CDS, UTR, 5' UTR, and 3' UTR records.}

\item{unit}{Output unit. \code{auto} uses gene-level records for genes,
transcript-level records for transcripts, transcript-level totals for CDS/UTR,
and segment-level records for exons. \code{transcript} summarizes exonic features
per transcript where applicable. \code{segment} returns individual exon/CDS/UTR
genomic segments where applicable.}

\item{transcript_length}{Transcript length definition. \code{spliced} uses the sum
of exon lengths. \code{genomic} uses transcript span length.}

\item{chrom}{Optional chromosome filter.}

\item{start}{Optional region start in 1-based closed coordinates.}

\item{end}{Optional region end in 1-based closed coordinates.}

\item{mode}{Region selection mode passed to \code{\link[=retrieve_feature]{retrieve_feature()}}.}

\item{keep_zero}{Logical. Whether to keep zero-length transcript-level CDS/UTR
records. Default is \code{FALSE}.}

\item{...}{Deprecated compatibility arguments forwarded by
\code{get_genepred_length_table()} to \code{get_gene_length_distribution_table()}.}
}
\value{
A data.table with feature-length records.
}
\description{
Build a tidy feature-length table from a Feature/GenePred-compatible annotation object. The function supports
gene spans, transcript lengths, exon lengths, CDS lengths, total UTR lengths,
5' UTR lengths, and 3' UTR lengths. CDS and UTR lengths can be summarized per
transcript or returned as individual genomic segments.
}
\details{
Use \code{unit = "segment"} to inspect each exon/CDS/UTR segment separately,
and \code{unit = "transcript"} to summarize feature lengths per transcript.
\code{transcript_length = "spliced"} uses exon lengths, whereas \code{"genomic"}
uses transcript span length including introns.
}
\examples{
gp <- read_genepred(
  system.file("extdata", "gtr_demo.genePredExt", package = "GeneTrackR"),
  format = "genePredExt", verbose = FALSE, progress = FALSE
)
get_gene_length_distribution_table(gp, feature = "cds", unit = "transcript")
}
